<% provide(:title, 'Log in') %>
<div id = "sign_up_header">
  <h3> 로그인 </h3>
    <div class="col-xs-12" id = "sign_up_box">
    <%= form_for(:session, url: login_path) do |f| %>
      
      <div class = "form-group">
        <%= f.label :email, "메일 주소", class: "col-xs-4 control-label btn" %>
          <div class = "col-xs-8">
          <%= f.email_field :email, class: 'form-control'%>
          </div>
        </div>

      <div class = "form-group">
        <%= f.label :password, "암호", class: "col-xs-4 control-label btn" %>
          <div class = "col-xs-8">
          <%= f.password_field :password, class: 'form-control' %>
          </div>
      </div>
      
      <div class = "col-xs-12">
        <%= f.label :remember_me, class: "checkbox inline" do %>
          <%= f.check_box :remember_me %>
          <span>로그인 상태 유지</span>
        <% end %>
      </div>

      <%= f.submit "로그인", class: "btn log_in_btn"  %>
    <% end %>
    
    <p>아직 회원이 아니세요? <%= link_to "가입하기", signup_path %></p>
  </div>
</div>